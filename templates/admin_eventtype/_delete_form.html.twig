<form style="display:inline-block" method="post" action="{{ path('app_admin_eventtype_delete', {'slug': event_type.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event_type.id) }}">
    <button class="btn"><i class="fa-solid fa-trash"></i></button>
</form>