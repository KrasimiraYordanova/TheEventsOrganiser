<form style="display:inline-block" method="post" action="{{ path('app_user_checklist_delete', {'id': eventList.id, 'checklist_id': checklist.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ checklist.id) }}">
    <button><i class="fa-solid fa-trash"></i></button>
</form>
