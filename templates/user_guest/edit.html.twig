{% extends 'base.html.twig' %}

{% block title %}Edit Guest{% endblock %}

{% block body %}
    <h1>Edit Guest</h1>

    {{ include('user_guest/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_user_guest_index') }}">back to list</a>

    {{ include('user_guest/_delete_form.html.twig') }}
{% endblock %}
